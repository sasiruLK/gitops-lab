apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: user-apps
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: tinycloud-platform
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: https://github.com/sasiruLK/gitops-lab.git
        revision: main
        directories:
          - path: apps/*
          # blog serves the apex domain and its manifests pin namespace
          # tinycloud, but this template would name the destination after the
          # directory — giving it namespace "blog", where nothing runs. The
          # generated Application then reported no pods.
          - path: apps/blog
            exclude: true
          - path: apps/tinycloud-api
            exclude: true
          - path: apps/tinycloud-ui
            exclude: true
          - path: apps/tinycloud-platform
            exclude: true
          - path: apps/monitoring-agents
            exclude: true
          - path: apps/tinycloud-demo
            exclude: true
          - path: apps/nginx
            exclude: true
          - path: apps/nginx-demo
            exclude: true
          - path: apps/_template
            exclude: true
  template:
    metadata:
      name: "{{.path.basename}}"
      labels:
        tinycloud.io/managed-by: platform
    spec:
      project: default
      source:
        repoURL: https://github.com/sasiruLK/gitops-lab.git
        targetRevision: main
        path: "{{.path.path}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{.path.basename}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
