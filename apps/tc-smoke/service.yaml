apiVersion: v1
kind: Service
metadata:
  name: tc-smoke
  labels:
    app.kubernetes.io/name: tc-smoke
    tinycloud.io/managed-by: platform
spec:
  selector:
    app.kubernetes.io/name: tc-smoke
  ports:
    - port: 80
      targetPort: http
      name: http
