apiVersion: v1
kind: Service
metadata:
  name: {{APP_NAME}}
  labels:
    app.kubernetes.io/name: {{APP_NAME}}
    tinycloud.io/managed-by: platform
spec:
  selector:
    app.kubernetes.io/name: {{APP_NAME}}
  ports:
    - port: 80
      targetPort: http
      name: http
