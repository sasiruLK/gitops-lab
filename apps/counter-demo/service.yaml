apiVersion: v1
kind: Service
metadata:
  name: counter-demo
  labels:
    app.kubernetes.io/name: counter-demo
    tinycloud.io/managed-by: platform
spec:
  selector:
    app.kubernetes.io/name: counter-demo
  ports:
    - port: 80
      targetPort: http
      name: http
