apiVersion: v1
kind: Service
metadata:
  name: monsters-rolodex
  labels:
    app.kubernetes.io/name: monsters-rolodex
    tinycloud.io/managed-by: platform
spec:
  selector:
    app.kubernetes.io/name: monsters-rolodex
  ports:
    - port: 80
      targetPort: http
      name: http
