apiVersion: v1
kind: Service
metadata:
  name: my-react-app
  labels:
    app.kubernetes.io/name: my-react-app
    tinycloud.io/managed-by: platform
spec:
  selector:
    app.kubernetes.io/name: my-react-app
  ports:
    - port: 80
      targetPort: http
      name: http
