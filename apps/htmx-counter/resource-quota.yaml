apiVersion: v1
kind: ResourceQuota
metadata:
  name: htmx-counter-quota
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "512Mi"
    limits.cpu: "1000m"
    limits.memory: "1Gi"
    pods: "5"
